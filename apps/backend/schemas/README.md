# schemas/

Pydantic schemas for data validation and serialization.

- `Request` / `Response` models for the API.
- Shared validation contracts with the frontend via `packages/shared/validation`.
- Keep serialization logic here; keep persistence in `models/`.
