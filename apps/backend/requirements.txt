# --- Web framework ---
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# --- Data validation / ORM ---
pydantic>=2.6.0
pydantic-settings>=2.2.0
email-validator>=2.1.0
sqlalchemy[asyncio]>=2.0.0
alembic>=1.13.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.0

# --- AI / LLM (placeholder deps; providers wired in Phase 1+) ---
# anthropic>=0.18.0
# openai>=1.12.0

# --- Security ---
# Argon2id password hashing (OWASP-recommended; spec-preferred over bcrypt).
argon2-cffi>=23.1.0
# JWT access/refresh tokens. PyJWT chosen over python-jose: narrower JWA
# surface, actively maintained, no transitive crypto bloat for our needs.
PyJWT>=2.8.0
cryptography>=42.0.0

# --- Dev / quality ---
pytest>=7.4.3
pytest-cov>=4.1.0
ruff>=0.4.0
mypy>=1.8.0
httpx>=0.27.0
