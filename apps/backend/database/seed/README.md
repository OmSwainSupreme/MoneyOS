# seed/

Idempotent seed scripts for local and development environments.

Reserved for reference data (categories, default thresholds, fixtures). Scripts
must be safe to re-run and must not contain real user PII.
